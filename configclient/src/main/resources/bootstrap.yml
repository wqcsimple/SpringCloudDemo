spring:
  application:
    name: client-api
  cloud:
    config:
      uri: http://localhost:8888
      profile: test
      label: master
server:
  port: 8889

management:
  security:
    enabled: true
